import { useEffect, useRef } from "react";
import { Renderer, Camera, Geometry, Program, Mesh } from "ogl";
import './Particles.css';

// ...then define the Particles component below...
const Particles = (props) => {
  // ...component code...
};

export default Particles;